apiVersion: v1
kind: Service
metadata:
  name: service-b-srv
  labels:
    app: service-b-app
    service: service-b-app-srv
spec:
  ports:
    - port: 80
      name: http-80
      targetPort: 8082
  selector:
    app: service-b-app